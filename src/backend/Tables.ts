export default class Tables {
  public static readonly SETTINGS = "settings" as const;
  public static readonly SUBSCRIPTION = "subscription" as const;
  public static readonly USERS = "users" as const;
}
